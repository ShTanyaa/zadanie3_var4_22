double massiv(int mas[], int n) {
    double sum = 0;
    for (int i = 0; i < n; i++) {
        sum += mas[i];
    }
    return sum / n;
}

double massiv(float mas[], int n) {
    double sum = 0;
    for (int i = 0; i < n; i++) {
        sum += mas[i];
    }
    return sum / n;
}

double massiv(double mas[], int n) {
    double sum = 0;
    for (int i = 0; i < n; i++) {
        sum += mas[i];
    }
    return sum / n;
}